<h1>New event</h1>

<%= error_messages_for :event %>

<% if current_user.has_role?("visitor") %>
  <%= render :partial => 'event_by_user' %>
<% end %>

<% if current_user.has_any_role?("innkeeper","staff","admin") %>
  <%= render :partial => 'event_by_authority' %>
<% end %>

<%= link_to 'Back', root_path %>
